# Changelog

All notable changes are recorded here, newest first. Versioning is **lockstep** — every `@greffon/*` package shares
one version, chosen at release time. Release sections below are generated by `scripts/release.mjs` from the
Conventional-Commit history; do not hand-edit released sections.

<!-- releases -->

## v0.1.1 — 2026-08-20

### ⚠ Breaking changes

- rename where/select operators to filter/map (linq)
- navigation predicates via EXISTS and JS-idiomatic executors (linq)
- compile joins and split-query includes via a layered builder (sql)

### Features

- evaluate the tree against a sample row (playground)
- add wire-filter — a predicate crossing a boundary as JSON (examples)
- add policy-rules — one rule as filter, check, and artifact (examples)
- add Monaco syntax highlighting and accurate error markers (playground)
- add computed members inlined into queries (linq)
- add makeExpr to build an Expr from a hand-built tree (core)
- rename where/select operators to filter/map (linq)
- reify query lambdas through the TypeScript compiler (ts-transformer)
- add synchronous transform and edit-list entry points (transform)
- load the rules into oxlint via jsPlugins and enforce them repo-wide (eslint-plugin)
- translate Date getFullYear/getMonth/getDate via a dateExtract seam (sql)
- flatMap over navigations (EF SelectMany) (linq)
- groupBy SQL translation and refined includes (linq)
- add a sample gallery over a related demo schema (playground)
- correlated counts and sums over navigations (linq)
- navigation predicates via EXISTS and JS-idiomatic executors (linq)
- compile joins and split-query includes via a layered builder (sql)
- first-class left joins and EF-style include/thenInclude (linq)
- add a SQLite dialect and provider (sql)
- add browser playground linked from the docs (playground)
- expose emitNode via a browser-safe ./emit subpath (transform)
- add in-editor subset diagnostics (ts-plugin)
- add subset lint rules with loose-equality autofix (eslint-plugin)
- add runtime toString capture path (fallback)
- add build transform and vite plugin (transform)
- add postgres provider compiling trees to parameterized SQL (sql)
- add query layer, provider protocol and in-memory oracle (linq)
- add shared subset validator and free-variable analysis (capture)
- add Expr host, visitor, partial evaluation and printer (core)
- add closed expression-tree wire format and serialization (tree)

### Fixes

- correct the Capabilities.ops example to a real op kind (query)
- declare the @greffon/tree dependency of the emit entry (transform)
- ignore global-namespace statics that share query-operator names (eslint-plugin)
- make playground links do a full page load instead of hitting the SPA 404 (docs)
- keep the method-call shape when only the receiver folds (core)
- make the transform benchmark advisory and gate on throughput (ci)
- make script CLI guards Windows-safe (tooling)

### Documentation

- show the TypeScript parser in the ESLint setup snippet
- explain lint/build enforcement and why the subset is not a type error
- add sqlite provider readme (sql)
- add postgres provider readme (sql)
- add sql-core package readme (sql)
- add package readme (memory)
- add package readme (query)
- log query engine plumbing placement finding
- describe the tree-reading navigation path accurately (memory)
- address the reader instead of implying a community
- add package readme (eslint-plugin)
- add package readme (ts-plugin)
- add package readme (fallback)
- add package readme (ts-transformer)
- add package readme (vite)
- add package readme (transform)
- drop a plan goal reference from the fallback example (examples)
- describe the lint rules' context tracing accurately (docs)
- fix jsdoc line breaks and name the lint among capture hosts
- add package readme (capture)
- add package readme (core)
- add package readme (tree)
- complete runtime dependency lists in the readme package table
- add concision, plain-words, and sparing-em-dash rules to the voice guide
- note the ts-transformer build path alongside the vite plugin in the lineage page (docs)
- organize the sidebar by reader task and fill the querying gaps (docs)
- present querying as one application, not the headline
- state the positioning without the not-an-ORM refrain
- record the EF-in-background voice rule
- restructure the README around the tree
- place Treequel next to rules engines and policy languages (docs)
- give the tree its own guide section (docs)
- lead every pinned surface with the tree-first positioning line
- describe the vite-postgres criterion without a plan reference (examples)
- add "Beyond queries" — the tree as a usable artifact (docs)
- add C# lineage and ORM comparison pages (docs)
- joins & includes guide, provider-kit updates, ADR-0004
- document the SQLite provider (sql)
- commit the design plan for agent handoff
- add VitePress site with generated error and tree-schema pages
- add dependency inventory, ADR and update guides
- add agent and contributor guides

### Internal changes

- add ESLint flat config alongside oxlint (examples)
- lint query lambdas with the greffon subset rules (examples)
- auto-enable git hooks via npm prepare (tooling)
- add tsconfig so IDEs resolve @greffon/* exports maps (examples)
- commit exploration findings so they survive across sessions
- add repository, homepage, and bugs metadata to package manifests
- sort package.json keywords after the rename to greffon
- rename the project and npm scope from Treequel to Greffon
- centralize the duplicated QUERY_METHODS set (capture)
- drop the positioning drift test
- format index.html (playground)
- point npm keywords at expression trees
- pin the positioning surfaces with a drift test
- rename @treequel/linq to @treequel/query (query)
- add @treequel/ts-transformer to the lockfile (deps)
- split include.ts by concern (linq)
- share provider fixtures through the linq testing kit (sql)
- rename provider-sql to sql-core (sql)
- split provider-sql god-files into cohesive modules (sql)
- add EF-inspired coverage for joins, includes and edges (linq)
- split providers into per-dialect packages over a shared core (sql)
- replace the memory-provider "oracle" term with "reference"
- add a weekly TypeScript/oxc canary and refresh repo status
- add code of conduct, GitHub issue forms and CODEOWNERS
- render the changelog and add the release workflow (release)
- add transform benchmark with a machine-independent regression gate (tooling)
- add conventional-commits linter and PR commit-lint job
- add fast-check property tests and F | Expr<F> type tests
- enforce LF line endings via .gitattributes
- format the codebase with oxfmt
- add oxlint and oxfmt and gate verify and CI on them
- add matrix workflow, dependabot and pull-request template
- add memory-vs-postgres and fallback integration examples (examples)
- scaffold npm-workspace monorepo, toolchain and dependency guard

