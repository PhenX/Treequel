See [AGENTS.md](AGENTS.md) for project context, repository structure, conventions and instructions.

The build follows the design & implementation plan at `plans/DESIGN.md` (local, gitignored, never committed) — read
the plan section covering the area you are editing, in addition to the root guide. The directory → plan-section
mapping table is at the top of [AGENTS.md](AGENTS.md).

Area guides are added as packages accrue rules of their own — check for an `AGENTS.md` inside the directory you are
editing.
