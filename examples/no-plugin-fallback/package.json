{
  "name": "@treequel-example/no-plugin-fallback",
  "version": "0.1.0",
  "private": true,
  "description": "Graceful degradation: in-memory works fully without the plugin; remote providers fall back to runtime parse with teachable closure errors.",
  "type": "module",
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "@treequel/fallback": "*",
    "@treequel/provider-memory": "*",
    "@treequel/provider-postgres": "*",
    "@treequel/query": "*"
  }
}
