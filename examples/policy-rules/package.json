{
  "name": "@greffon-example/policy-rules",
  "version": "0.1.0",
  "private": true,
  "description": "Policy-as-expression: one visibility rule as a list-query WHERE, a single-object check, and a stored, auditable artifact.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "lint": "eslint .",
    "lint:oxlint": "oxlint"
  },
  "dependencies": {
    "@greffon/core": "*",
    "@greffon/provider-memory": "*",
    "@greffon/provider-postgres": "*",
    "@greffon/query": "*",
    "@greffon/tree": "*"
  },
  "devDependencies": {
    "@greffon/eslint-plugin": "*",
    "@greffon/vite": "*",
    "@typescript-eslint/parser": "^8.0.0",
    "eslint": "^10.8.1"
  }
}
