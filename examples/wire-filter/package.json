{
  "name": "@treequel-example/wire-filter",
  "version": "0.1.0",
  "private": true,
  "description": "A saved-search predicate crossing a process boundary as JSON: serialize, re-validate, evaluate live events, and backfill as a query.",
  "type": "module",
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "@treequel/core": "*",
    "@treequel/provider-memory": "*",
    "@treequel/query": "*",
    "@treequel/tree": "*"
  },
  "devDependencies": {
    "@treequel/vite": "*"
  }
}
