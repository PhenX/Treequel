{
  "name": "@greffon-example/wire-filter",
  "version": "0.1.0",
  "private": true,
  "description": "A saved-search predicate crossing a process boundary as JSON: serialize, re-validate, evaluate live events, and backfill as a query.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "lint": "eslint .",
    "lint:oxlint": "oxlint"
  },
  "dependencies": {
    "@greffon/core": "*",
    "@greffon/provider-memory": "*",
    "@greffon/query": "*",
    "@greffon/tree": "*"
  },
  "devDependencies": {
    "@greffon/eslint-plugin": "*",
    "@greffon/vite": "*",
    "@typescript-eslint/parser": "^8.0.0",
    "eslint": "^9.0.0"
  }
}
