{
  "name": "@treequel/core",
  "version": "0.1.0",
  "description": "Treequel runtime core: Expr, visitor/rewriter, partial evaluation, printer, WellKnown registry.",
  "keywords": [
    "treequel",
    "expression-tree",
    "linq",
    "partial-evaluation"
  ],
  "license": "MIT",
  "type": "module",
  "sideEffects": false,
  "engines": {
    "node": ">=20"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "dependencies": {
    "@treequel/tree": "*"
  },
  "scripts": {
    "build": "tsdown src/index.ts --format esm --dts",
    "dev": "tsdown src/index.ts --format esm --dts --watch",
    "test": "vitest run"
  }
}
