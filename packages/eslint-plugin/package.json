{
  "name": "@treequel/eslint-plugin",
  "version": "0.1.0",
  "description": "ESLint rules enforcing the Treequel expression subset (shares the capture validator).",
  "keywords": [
    "treequel",
    "eslint",
    "eslint-plugin",
    "linq"
  ],
  "license": "MIT",
  "type": "module",
  "sideEffects": false,
  "engines": {
    "node": ">=20"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "dependencies": {
    "@treequel/capture": "*"
  },
  "peerDependencies": {
    "eslint": ">=8"
  },
  "devDependencies": {
    "@typescript-eslint/parser": "^8.0.0",
    "eslint": "^9.0.0"
  },
  "scripts": {
    "build": "tsdown src/index.ts --format esm --dts",
    "dev": "tsdown src/index.ts --format esm --dts --watch",
    "test": "vitest run"
  }
}
