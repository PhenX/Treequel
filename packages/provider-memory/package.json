{
  "name": "@treequel/provider-memory",
  "version": "0.1.0",
  "description": "Treequel reference provider — runs queries in memory via compiled lambdas.",
  "keywords": [
    "in-memory",
    "linq",
    "provider",
    "treequel"
  ],
  "license": "MIT",
  "files": [
    "dist"
  ],
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "scripts": {
    "build": "tsdown src/index.ts --format esm --dts",
    "dev": "tsdown src/index.ts --format esm --dts --watch",
    "test": "vitest run"
  },
  "dependencies": {
    "@treequel/core": "*",
    "@treequel/query": "*"
  },
  "engines": {
    "node": ">=20"
  }
}
