{
  "name": "@greffon/provider-sqlite",
  "version": "0.1.1",
  "description": "Greffon SQLite provider — compiles expression trees to parameterized SQLite.",
  "keywords": [
    "greffon",
    "linq",
    "provider",
    "sql",
    "sqlite"
  ],
  "homepage": "https://phenx.github.io/Greffon/",
  "bugs": "https://github.com/PhenX/Greffon/issues",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/PhenX/Greffon.git",
    "directory": "packages/provider-sqlite"
  },
  "files": [
    "dist"
  ],
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "scripts": {
    "build": "tsdown src/index.ts --format esm --dts",
    "dev": "tsdown src/index.ts --format esm --dts --watch",
    "test": "vitest run"
  },
  "dependencies": {
    "@greffon/sql-core": "^0.1.1"
  },
  "engines": {
    "node": ">=20"
  }
}
