{
  "name": "@treequel/provider-sqlite",
  "version": "0.1.0",
  "description": "Treequel SQLite provider — compiles expression trees to parameterized SQLite.",
  "keywords": [
    "linq",
    "provider",
    "sql",
    "sqlite",
    "treequel"
  ],
  "license": "MIT",
  "files": [
    "dist"
  ],
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "scripts": {
    "build": "tsdown src/index.ts --format esm --dts",
    "dev": "tsdown src/index.ts --format esm --dts --watch",
    "test": "vitest run"
  },
  "dependencies": {
    "@treequel/provider-sql": "*"
  },
  "engines": {
    "node": ">=20"
  }
}
